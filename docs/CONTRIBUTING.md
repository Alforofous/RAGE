# Contributing

## Commits

Semantic commit messages with bullet-point body:

```
<type>(<scope>): <subject>

- bullet point
- bullet point
```

Types: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `build`, `ci`, `perf`

Scope is optional. Subject in present tense ("add" not "added").
